diff --git a/app/main.py b/app/main.py
index a59bdf0..91fea6c 100644
--- a/app/main.py
+++ b/app/main.py
@@ -6,6 +6,7 @@ from typing import List
 from app.database import supabase
 from app.config import settings
 from app.excel_generator import XLSX_MIME, build_formula_workbook
+from app.efek_samping import _efek_samping_meta, register_efek_samping_routes
 from decimal import Decimal, ROUND_HALF_UP
 from datetime import datetime, date, timedelta
 from zoneinfo import ZoneInfo
@@ -367,6 +368,7 @@ COMPANY_INFO = {
         "website": "www.erfikaryaabadi.com",
         "logo": "/static/images/logo_erfi.png",
         "kop": "/static/images/kop_erfi.png",
+        "penanggung_jawab_teknis": "Apt. Mutrofin Rakhmawati, S.Farm",
     },
     "PT Heka": {
         "nama": "PT. HARAKA ERFI KOSMETINDO ABADI",          
@@ -375,6 +377,7 @@ COMPANY_INFO = {
         "website": "www.harakaerfi.com",
         "logo": "/static/images/logo_heka.png",
         "kop": "/static/images/kop_heka.png",
+        "penanggung_jawab_teknis": "Apt. Mutrofin Rakhmawati, S.Farm",
     }
 }
 
@@ -4050,6 +4053,7 @@ async def edit_product_page(request: Request, product_id: str, current_user: dic
 
     # Get error message from cookie (if any)
     error_msg = request.cookies.get("error_msg")
+    success_msg = request.cookies.get("success_msg")
 
     return templates.TemplateResponse(
         request=request,
@@ -4062,6 +4066,8 @@ async def edit_product_page(request: Request, product_id: str, current_user: dic
             "bab2_materials": bab2_materials,
             "sop_cpkb_url": sop_cpkb_url,
             "error_msg": error_msg,
+            "success_msg": success_msg,
+            "efek_samping": _efek_samping_meta(product),
             "current_user": current_user,
         }
     )
@@ -5274,3 +5280,13 @@ async def delete_user(
 from app.notes_routes import register_notes_routes
 register_notes_routes(app, get_current_user, get_ed_notification_count, log_activity)
 
+register_efek_samping_routes(
+    app,
+    get_current_user=get_current_user,
+    templates=templates,
+    supabase=supabase,
+    get_company_info=get_company_info,
+    format_date_id=format_date_id,
+    log_activity=log_activity,
+    pdf_link_callback=_pdf_link_callback,
+)
+diff --git a/app/templates/edit_product.html b/app/templates/edit_product.html
index 908b6ae..ca595e7 100644
--- a/app/templates/edit_product.html
+++ b/app/templates/edit_product.html
@@ -94,6 +94,18 @@
     </script>
     {% endif %}
 
+    {% if success_msg or request.cookies.get('success_msg') %}
+    <div class="bg-emerald-50 border border-emerald-200 text-emerald-800 px-4 py-3 rounded-md mb-4 text-sm flex items-center gap-2" role="status">
+        <i class="fa-solid fa-circle-check"></i>
+        <span>{{ success_msg or request.cookies.get('success_msg') }}</span>
+    </div>
+    <script>
+        document.addEventListener('DOMContentLoaded', function() {
+            document.cookie = 'success_msg=; expires=Thu, 01 Jan 1970 00:00:00 UTC; path=/;';
+        });
+    </script>
+    {% endif %}
+
     <!-- ================= NAVIGASI TABS ================= -->
     <div class="border-b border-gray-200 mb-6">
         <nav class="-mb-px flex space-x-6" aria-label="Tabs">
@@ -474,7 +486,18 @@
         <!-- TAB 4: DOKUMEN BAB 4 (KEAMANAN & KLAIM) -->
         <div id="tab-bab4" class="edit-tab-content hidden space-y-4">
             {{ pdf_upload_card('1. Laporan Penilaian Keamanan Kosmetik (Safety Assessment)', 'laporan_keamanan_file', product.laporan_keamanan_file_url, 'Dokumen evaluasi & penilaian keamanan produk dari Qualified Safety Assessor.') }}
-            {{ pdf_upload_card('3. Laporan / Data Monitoring Efek Samping (Cosmetovigilance)', 'monitoring_efek_samping_file', product.monitoring_efek_samping_file_url, 'Catatan/rekapan evaluasi efek tidak diinginkan khusus untuk produk ini.') }}
+            {{ pdf_upload_card('3. Laporan / Data Monitoring Efek Samping (Cosmetovigilance)', 'monitoring_efek_samping_file', product.monitoring_efek_samping_file_url, 'Catatan/rekapan evaluasi efek tidak diinginkan khusus untuk produk ini. Generate per semester akan menambahkan blok baru di PDF yang sama (append).') }}
+            <div class="p-4 bg-white rounded-lg border border-indigo-100 -mt-2">
+                <div class="flex flex-wrap items-center justify-between gap-3">
+                    <div class="text-xs text-gray-600 space-y-0.5">
+                        <p>Periode terakhir: <span class="font-semibold text-gray-900">{{ efek_samping.last_label or 'belum ada' }}{% if efek_samping.last %} <span class="font-mono text-gray-500">({{ efek_samping.last }})</span>{% endif %}</span></p>
+                        <p>Periode berikutnya: <span class="font-semibold text-indigo-700">{{ efek_samping.next_label }} <span class="font-mono">({{ efek_samping.next }})</span></span></p>
+                    </div>
+                    <button type="button" onclick="openEfekSampingModal()" class="inline-flex items-center gap-1.5 px-3 py-2 text-xs font-semibold text-white bg-indigo-600 rounded-md hover:bg-indigo-700">
+                        <i class="fa-solid fa-file-circle-plus"></i> Generate Laporan Periode Ini
+                    </button>
+                </div>
+            </div>
             {{ pdf_upload_card('4. Data Pendukung Klaim Kosmetik (Claim Substantiation)', 'data_klaim_file', product.data_klaim_file_url, 'Dokumen bukti klaim (Uji Klinis, Dermatologically Tested, Patch Test, Jurnal Efikasi).') }}
             {{ pdf_upload_card('5a. Desain Kemasan Primer (Penandaan)', 'desain_primer_file', product.desain_primer_file_url, 'Layout/Desain cetak fisik kemasan primer (botol/jar/tube).') }}
             {{ pdf_upload_card('5b. Desain Kemasan Sekunder (Penandaan)', 'desain_sekunder_file', product.desain_sekunder_file_url, 'Layout/Desain cetak fisik kemasan luar (Dus/Box).') }}
@@ -489,6 +512,8 @@
 </div>
 
 
+{% include 'efek_samping_modal.html' %}
+
 <div id="fileToast" class="fixed bottom-6 right-6 z-[60] hidden items-center gap-2 bg-red-600 text-white text-sm px-4 py-3 rounded-lg shadow-xl">
     <i class="fa-solid fa-circle-exclamation text-white"></i>
     <span></span>
@@ -1112,4 +1137,5 @@ document.getElementById('quick-add-brand-form').addEventListener('submit', async
 });
 
 </script>
+<script src="/static/efek_samping.js"></script>
 {% endblock %}
